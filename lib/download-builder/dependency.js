/*
 * dependency.js: Combined state for the build of a single jQuery UI plugin. 
 *
 * (C) 2011 Charlie Robbins and jQuery UI contributors
 * MIT LICENCE
 *
 */
 
var Dependency = exports.Dependency = function Dependency (build, options) {
  options = options || {};
  this.repository = options.repository;
};

Dependency.prototype.build = function (callback) {
  //
  // TODO: Finish this method
  //
  callback();
};
